<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Personajes</h1>
    <a href="/characters/create">Agregar personaje</a>
    <% if(characters) { %>
        <ul>
            <% characters.forEach(e => { %>
            <a href="/characters/<%= e.id %>"><li><%= e.name %></li></a>
            <% }) %>
        </ul>
    <% } %>
    <a href="/">Home</a>
</body>
</html>