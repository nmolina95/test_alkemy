<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Géneros</h1>
    <a href="/genres/add">Agregar categoría</a>
    <% if(genres) { %>
        <div>
            <ul>
                <% genres.forEach(e => { %>
                    <a href="/genres/<%=e.id%>"><li><%=e.name%></li></a>
                <% }) %>
            </ul>
        </div>
    <% } else { %>
        <p>Aún no se han agregado categorías</p>
    <% } %>
    <a href="/">Home</a>
</body>
</html>