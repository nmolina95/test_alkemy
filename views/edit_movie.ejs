<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Editar película/serie</h1>
    <form action="/movies/edit/<%= movie.id %>" method="post" enctype="application/x-www-form-urlencoded">
        <label for="title">
            <input type="text" name="title" value="<%= movie.title %>">
        </label>
        <label for="date">
            <input type="date" name="date" value="<%= movie.date %>">
        </label>
        <label for="image">
            <input type="text" name="image" value="<%= movie.image %>">
        </label>
        <label for="rating">
            <input type="number" name="rating" value="<%= movie.rating %>" min="1" max="5">
        </label>
        <% if(genres) { %>
            <label for="genre">
                <select name="genre">
                    <% for(let i = 0; i < genres.length ; i++){ %>
                        <% if(genres[i].id == movie.idGenre) {%>
                            <option value="<%=genres[i].id%>" selected><%=genres[i].name%></option>
                        <% } else { %>
                            <option value="<%=genres[i].id%>"><%=genres[i].name%></option>
                        <% } %>
                    <% } %>
                </select>
            </label>
            <% } else { %>
                <p>Aún no existen géneros</p>
            <% } %>
        <button type="submit">Enviar</button>
        <a href="/">Home</a>
    </form>
</body>
</html>